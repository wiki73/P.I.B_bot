# 📅 P.I.B_bot — Telegram-бот для планирования задач

## 📝 Описание

**P.I.B_bot** — это Telegram-бот, созданный для помощи пользователям в планировании своего дня. Бот позволяет удобно создавать, редактировать и публиковать списки задач, а также использовать их в личных и групповых чатах.

## ⚙️ Возможности

- Регистрация и сохранение данных пользователя
- Создание новых планов с заголовками и содержанием
- Редактирование уже созданных планов
- Отправка планов в личные и групповые чаты
- Работа с базой данных SQLite для хранения данных

## 📦 Установка

### Требования:

- Python 3.7+
- pip
- Docker и Docker Compose (опционально)

### Установка вручную:

```bash
git clone https://github.com/wiki73/P.I.B_bot.git
cd P.I.B_bot
pip install -r requirements.txt
python bot.py
```

### Запуск с использованием Docker:

```bash
docker-compose up --build
```

## 📁 Структура проекта

- `bot.py` — запуск и инициализация бота
- `database.py` — взаимодействие с SQLite базой данных
- `handlers/` — обработчики пользовательских команд и сообщений
- `keyboards/` — пользовательские клавиатуры и меню
- `middlewares/` — промежуточная логика между обработчиками
- `states/` — описание состояний конечного автомата (FSM)
- `utils.py` — вспомогательные функции
- `Dockerfile`, `docker-compose.yml` — конфигурация для контейнеризации

## 🗃️ База данных

Бот использует SQLite-файл `plans.db` для хранения информации о пользователях и их планах. Файл создаётся автоматически при первом запуске.

## 🙌 Вклад в проект

Вы можете внести свой вклад, предложив улучшения, исправив ошибки или добавив новые функции. Просто создайте Pull Request или откройте Issue на GitHub.

## 📃 Лицензия

Проект распространяется под лицензией MIT. Подробнее см. файл [LICENSE](LICENSE).